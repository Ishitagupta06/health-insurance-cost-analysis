# Health Insurance Charges — Regression Analysis

Looking at what drives individual health insurance costs, using an interpretable OLS regression: formal hypothesis testing, an interaction term motivated by a pattern found in the data, and classical regression assumption checks.

## Key Findings

- **Smoking status is the dominant cost driver.** Median charges are **$34,545 for smokers vs. $7,419 for non-smokers** — confirmed with a Mann-Whitney U test (p ≈ 9.7e-118).
- **The effect of BMI depends on smoking status.** Adding an interaction term for this raised the model's R² from 0.705 to **0.842** and cut RMSE by 27% ($6,320 → $4,617).
- **Region has no statistically significant effect** on charges (Kruskal-Wallis p ≈ 0.20).
- **6 of 10 model coefficients are statistically significant at p < 0.05** (age, smoker, bmi, children, and both engineered interaction features) — confirmed with `statsmodels.OLS`. Region and sex are not significant, consistent with the Kruskal-Wallis result above.

## Repository Structure

```
├── README.md
├── requirements.txt
├── data/
│   ├── insurance.csv                  # raw data
│   ├── validation_dataset.csv         # unseen data for scoring
│   └── insurance_clean.csv            # generated by scripts/01_data_cleaning.py
├── notebooks/
│   └── insurance_charges_analysis.ipynb   # full analysis, cell-by-cell
├── scripts/                           # the same pipeline, as standalone modules
│   ├── utils.py                       # shared cleaning / feature engineering
│   ├── 01_data_cleaning.py            # null/category/type cleanup, sign-error correction
│   ├── 02_eda.py                      # distributions, boxplots, correlation, interaction plot
│   ├── 03_hypothesis_tests.py         # Shapiro-Wilk, Mann-Whitney U, Kruskal-Wallis
│   ├── 04_model_comparison.py         # baseline vs. interaction vs. semi-log OLS
│   ├── 05_residual_analysis.py        # OLS assumption checks (homoscedasticity, normality)
│   ├── 06_validate.py                 # scores validation_dataset.csv
│   └── 07_coefficient_significance.py # statsmodels OLS: p-values on every coefficient
├── outputs/                           # generated by the scripts above
│   ├── figures/
│   ├── model_comparison.csv
│   ├── model_b_coefficients.csv
│   ├── coefficient_significance.csv
│   └── validation_predictions.csv
└── docs/
    ├── interview_prep_notes.md        # section-by-section: theory, code, purpose, results, Q&A
    └── econometrics_theory_notes.md   # pure theory reference (no code/dataset specifics)
```

## Setup

```bash
pip install -r requirements.txt
```

## Running

**Option A — the notebook** (`notebooks/insurance_charges_analysis.ipynb`): the full analysis end-to-end, cell by cell, with all interpretation written out alongside the code.

**Option B — the scripts**, run in order from the repo root:

```bash
python scripts/01_data_cleaning.py
python scripts/02_eda.py
python scripts/03_hypothesis_tests.py
python scripts/04_model_comparison.py
python scripts/05_residual_analysis.py
python scripts/06_validate.py
python scripts/07_coefficient_significance.py
```

Each script is independently runnable (after step 1 has produced `data/insurance_clean.csv`) and writes its output to `outputs/`.

## Tech Stack

**Python** end-to-end: pandas and NumPy for cleaning and data manipulation, scikit-learn and statsmodels for modeling and inference, SciPy for hypothesis testing, matplotlib/seaborn for visualization.

## Methodology Notes

- **Cleaning** is handled in pandas: dropping nulls, standardizing category labels, stripping the `$` prefix from `charges`, and correcting sign errors — with a re-validation step after every type coercion (this is what catches a hidden `"$nan"` string bug that survives an initial `.isna()` check).
- **Non-parametric hypothesis tests** (Mann-Whitney U, Kruskal-Wallis) are used instead of a t-test/ANOVA because a Shapiro-Wilk test confirms `charges` is not normally distributed — using the parametric versions here would violate their own assumptions.
- **Model choice** is based on RMSE/MAE in the target's original units (dollars), not just R², since R² alone can mask exactly where a model's errors are concentrated — see the residual analysis in Section 6 of the notebook.
- Full reasoning behind every choice, plus anticipated interview questions and answers, is in `docs/interview_prep_notes.md`.
